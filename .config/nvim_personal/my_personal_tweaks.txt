On "keymaps.lua" activate:
-- Insert --
-- Press jk fast to enter
vim.keymap.set("i", "jk", "<ESC>")

On options.lua
vim.opt.listchars={space = '.', tab = '>~', eol='↵'}

Commented the plugins that are not working properly:
-- require "user.sniprun"

Comment the lines that give trouble on winbar
and let's go!
----
1. comment on lualine.lua the line where StatusLineSeparator is called which is not previously defined
2. comment user.sniprun on init.lua
3. comment the trouble lines on winbar.lua (navic_text lines)
